# Esri Rwanda portal credentials.
# NO VITE_ PREFIX — these must never reach the browser bundle. They are read
# only by the /api/arcgis-token middleware in vite.config.ts.
ESRI_USERNAME=
ESRI_PASSWORD=

# Origin the portal token is bound to. Must match how you open the app.
APP_ORIGIN=http://localhost:5173
